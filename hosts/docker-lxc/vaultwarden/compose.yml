version: '3'

services:
  vaultwarden:
    image: vaultwarden/server:latest
    container_name: vaultwarden
    restart: always
    env_file:
      - ./.env
      - ./.env.local
    ports:
      - "8100:80"
      - "3012:3012"
    volumes:
      - ./vw-data:/data